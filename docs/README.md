# VoiceBridge — документация проекта

Голосовой ассистент для разработчиков. Десктоп-приложение (macOS + Windows) на
стеке Rust + Tauri, живёт в системном трее. Распознаёт речь локально (без
интернета) и управляет OpenCode.

## Модель интерфейса

- **Главное окно** — лаунчер: список проектов (запуск/остановка серверов
  OpenCode, «＋ Новый» с выбором папки) и список сессий запущенных экземпляров.
  Клик по сессии открывает окно чата. Сессии с открытым окном помечаются
  бейджем «открыта»; при закрытии окна выбор сессии сбрасывается.
- **Окно чата** (по одному на сессию) — основной интерфейс общения: сверху
  название сессии и имя проекта, ниже полный диалог («Вы» / «OpenCode» с
  markdown и подсветкой синтаксиса), инструменты, запросы действий
  (разрешения/вопросы — в отдельном «доке» над полем ввода, всегда на виду),
  история сессии. У каждого ответа — кнопка копирования и время выполнения
  (тикает в реалтайме и замирает по завершении). Пока OpenCode думает, в строке
  состояния над полем ввода показывается статус («работает…» / «выполняет
  `read`…»); если событий нет дольше 90 с — предупреждение «возможно, завис» с
  кнопкой прервать. Внизу — панель ввода: текстовое поле + кнопка отправки +
  голосовая кнопка (тап/удержание). Ниже — строка состояния: токены сессии и
  стоимость. Слева — скрываемая панель сообщений (список запросов пользователя
  с превью, клик — переход к сообщению с подсветкой). Справа (или оверлеем на
  узком окне) — скрываемая Git-панель с вкладками:
  «Изменения» (изменённые файлы, сгруппированные по папкам, с диффом по клику),
  «История» (список коммитов; клик по коммиту — изменённые файлы и дифф коммита)
  и «Ветки» (список локальных веток, текущая помечена). Обе панели
  переключаются кнопками в шапке (рядом справа). Окна изолированы друг от
  друга по сессиям.

## Режим работы

Наговоренный текст отправляется как промпт в выбранную сессию OpenCode, ответ
стримится в окно чата.

## Стек

- **Бэкенд**: Rust, Tauri 2 (`src-tauri/`).
- **Фронтенд**: React 19 + TypeScript + Vite 7 (`src/`).
- **Захват звука**: `cpal` 0.18.
- **Распознавание речи**: `whisper.cpp` через `whisper-rs` 0.16 (Metal на Apple
  Silicon).
- **HTTP**: `reqwest` (blocking + json).
- **Markdown**: `react-markdown` + `remark-gfm` + `rehype-highlight`
  (подсветка синтаксиса).
- **Иконки**: `lucide-react`.
- **Звуки**: Web Audio API (синтез на лету, без аудиофайлов).

## Структура

```
src/                        фронтенд (React + TS)
  App.tsx                   главное окно (MainApp) + окно чата (ResponseView)
  ChatInput.tsx             панель ввода в чате (текст + отправка + голос)
  Markdown.tsx              рендер markdown
  sounds.ts                 звуки интерфейса (Web Audio API)
  types.ts                  типы (общие с бэкендом)
  styles.css                стили (тёмная тема)
src-tauri/src/              бэкенд
  lib.rs                    сборка: плагины, состояние, трей, хоткеи, команды
  commands.rs               Tauri-команды (мост фронтенд ↔ модули)
  logging.rs                файловый логгер + перехват паник
  state.rs                  AppState, ConversationStore, типы
  modules/audio.rs          захват cpal, буфер, ресемплинг в 16 кГц
  modules/stt.rs            whisper, реестр моделей, скачивание, поток STT
  modules/opencode.rs       интеграция с OpenCode (обнаружение, сессии,
                            проекты, стриминг, диалоги, действия)
  modules/mobile.rs         мобильный доступ (WebSocket-сервер, токен/QR)
src-tauri/examples/         whisper_check, opencode_check, projects_check
scripts/gen_icon.py         генерация иконки
scripts/build-desktop.sh    сборка установщика десктопа (текущая ОС)
scripts/build-android.sh    сборка Android APK (release)
scripts/gen_updater_json.py генерация latest.json (манифест автообновления)
```

## Поток данных

1. Запись (в окне чата): `cpal` пишет сэмплы в буфер + считает RMS-уровень →
   событие `audio-level` (волна в UI).
2. Остановка → буфер ресемплится в 16 кГц mono → задание в STT-поток.
3. STT-поток (владеет `WhisperContext`) транскрибирует → `finish_transcription`.
4. В режиме OpenCode → `send_prompt` → `POST /session/{id}/message` + чтение
   SSE `GET /event` → стриминг ответа в окно чата.

## Запись и ввод

Голосовая кнопка в окне чата: **тап** = переключение (старт/стоп), **удержание** =
запись пока держишь (отпустил — стоп). Есть авто-остановка по тишине (настройка
в «Настройки → Микрофон»). Глобальный хоткей `⌘⇧V` / `Ctrl+Shift+V` тоже
работает и настраивается в «Настройки → Горячие клавиши».

Рядом — текстовое поле: сообщение можно набрать и отправить кнопкой отправки
или Enter.

**Режимы отправки** (настройка «Настройки → OpenCode → Режим отправки»):
- **Сразу в чат** — распознанный текст сразу уходит в сессию.
- **Предпросмотр** — распознанный текст попадает в поле ввода, отправляешь сам.

Текущий режим показывается бейджем в панели ввода; клик по бейджу переключает
режим. При «сразу в чат» текстовое поле скрыто.

## Сессии изолированы

Окна чата независимы друг от друга. Запись, индикатор записи и превью
распознанного текста привязаны к сессии конкретного окна (поле
`recording_session_id` в `AppState`), а не к глобальному состоянию. Хоткей и
трей-меню без окна используют выбранную в лаунчере сессию.

## Звуки и индикатор ответа

- Начало/конец записи, отправка сообщения и получение ответа сопровождаются
  короткими синтезированными звуками (Web Audio API, `src/sounds.ts`).
- Пока OpenCode формирует ответ, в чате показывается анимированный индикатор
  «думает» (три точки).

## OpenCode

Подробности API и типы событий — см. `docs/OPENCODE.md`.
Коротко:

- Обнаружение: процессы `opencode` через `lsof` (macOS/Linux) или
  `netstat`+`tasklist` (Windows) + порты 4096/12000/3000/17000.
- Сессии: `GET /session`; история: `GET /session/{id}/message`.
- Отправка: `POST /session/{id}/message`; стрим — SSE `GET /event`.
- Проекты: список из БД (`opencode db`), запуск/остановка `opencode serve`
  из приложения (стабильный порт по хэшу пути) + создание нового инстанса с
  произвольной рабочей папкой. Папки, запущенные через «Новый проект» (ещё без
  сессий в БД), запоминаются в `AppState.known_worktrees` и подмешиваются к
  списку (`list_projects_with_extra`), чтобы не пропадать при обновлении.
- Действия: запросы разрешений (`permission.asked`) и вопросы (`question.asked`)
  обрабатываются в окне чата (разрешить/запретить, варианты ответа).

## Сборка и запуск

```bash
npm install
npm run tauri dev     # dev
npm run tauri build   # релиз (.dmg / .exe)
scripts/build-android.sh   # Android APK (см. docs/BUILD.md)
```

Требования: Rust (rustup), Node 20+, cmake (для whisper.cpp), Xcode CLT
(macOS) или MSVC Build Tools + LLVM (`libclang.dll` для bindgen whisper-rs,
`LIBCLANG_PATH` на `<LLVM>\bin`) (Windows). Первая сборка компилирует
whisper.cpp (несколько минут).

Полная матрица сборки (включая Windows через CI и Android) — в
[`docs/BUILD.md`](BUILD.md).

## Автообновление

Подключён `tauri-plugin-updater`. Конфиг в `src-tauri/tauri.conf.json`
(`plugins.updater`): endpoint — `latest.json` из GitHub Releases, вшит
публичный ключ подписи. Проверка обновлений — команда `check_update`
(кнопка «Проверить обновления» в «Настройки → О программе»).

Ключ подписи:

- приватный — `src-tauri/voicebridge-signing.key` (в `.gitignore`, не коммитить);
- в CI приватный ключ передаётся через env `TAURI_SIGNING_PRIVATE_KEY`
  (секрет репозитория `TAURI_SIGNING_PRIVATE_KEY`).

`bundle.createUpdaterArtifacts` **включён** — сборка генерирует подписи
`.sig`, а CI (`.github/workflows/build.yml`) собирает из них `latest.json`
(скрипт `scripts/gen_updater_json.py`) и прикрепляет его к релизу. Endpoint
указывает на `releases/latest/download/latest.json`.

**Windows** — автообновление работает (NSIS `.exe` + `installMode: passive`).
**macOS** — пока не готово: нужны подпись Developer ID + нотаризация `.app`,
иначе Gatekeeper блокирует обновление (в `latest.json` публикуется только
`windows-x86_64`).

> Сборка с включённым `createUpdaterArtifacts` требует приватный ключ:
> локально `scripts/build-desktop.sh` подставляет
> `TAURI_SIGNING_PRIVATE_KEY_PATH`, в CI — секрет
> `TAURI_SIGNING_PRIVATE_KEY`.

## Логи

Лог пишется в папку логов приложения (`app_log_dir()`): `voicebridge.log`
- macOS: `~/Library/Logs/com.voicebridge.app/voicebridge.log`
- Windows: `%LOCALAPPDATA%\com.voicebridge.app\logs\voicebridge.log`
  (обычно `C:\Users\<имя>\AppData\Local\com.voicebridge.app\logs\`) — это
  **Local** AppData, а не Roaming (`%APPDATA%`).

Туда пишутся старт, загрузка/скачивание модели, транскрипция, обнаружение
инстансов, ошибки и паники. Уровень — `Info` (debug-сообщения не пишутся).

## Известные особенности / грабли

- `src-tauri/.cargo/config.toml` принудительно ставит `SDKROOT=""` и
  `MACOSX_DEPLOYMENT_TARGET=10.15`. Первое — иначе при `SDKROOT=iPhoneOS` сборка
  whisper.cpp падает на FindBLAS; второе — иначе в новых Xcode сборка падает на
  `std::filesystem` (требует macOS 10.15+).
- Типы событий SSE OpenCode **не совпадают** с OpenAPI-схемой (см.
  `docs/OPENCODE.md`).
- Обычный TUI `opencode` не открывает TCP-порт — нужно `opencode serve` или
  `opencode web`.
- Capability `windows: ["*"]` обязательна, чтобы динамические окна
  `response-{sessionId}` получали события.

## Планы

Выполнено:

- ✅ **Рефакторинг под современную архитектуру** — Feature-Sliced Design на
  фронтенде (`app/`, `pages/`, `features/`, `shared/`), модули по доменам на
  бэкенде (`commands/`, `modules/opencode/`), виджеты мобилки, CSS разнесён по
  FSD. Ветка `refactoring`.
- ✅ **Git-панель изменений** (десктоп + мобилка) — список изменённых файлов
  (сгруппированы по папкам), дифф по клику, текущая ветка, responsive
  (встроенная панель / оверлей по кнопке), отдельный экран в мобилке.
- ✅ **Список коммитов в Git-панели** (десктоп + мобилка) — вкладка «История»
  в Git-панели: история коммитов (`git log`, хэш/автор/дата/сообщение), клик по
  коммиту — изменённые файлы и дифф коммита (`git show`). В мобилке —
  переключатель «Изменения»/«История» и отдельный экран коммита.
- ✅ **Просмотр веток в Git-панели** (десктоп + мобилка) — вкладка «Ветки»:
  список локальных веток (`git for-each-ref refs/heads`), текущая ветка помечена
  бейджем «текущая».
- ✅ **Панель сообщений (десктоп)** — скрываемая левая панель со списком
  запросов пользователя (превью), клик — переход к сообщению с подсветкой; на
  узком окне — шторка слева.
- ✅ **Восстановление ответа в мобилке при возврате** — стриминговый ответ
  больше не теряется при сворачивании приложения: при возврате из фона (и после
  переподключения) диалог подтягивается из истории десктопа/OpenCode
  (`get_conversation`), а индикатор «думает» снимается, если генерация
  завершилась. `AppController` подписан на жизненный цикл
  (`WidgetsBindingObserver`, `didChangeAppLifecycleState`).
- ✅ **Умный автоскролл чата** (десктоп + мобилка) — автоскролл к последнему
  сообщению работает только когда пользователь у низа; при прокрутке вверх
  (чтение истории/начала ответа) позиция не трогается. Появляется кнопка
  «К последнему сообщению», когда пользователь не внизу.
- ✅ **Управление сессиями** (десктоп + мобилка) — удаление сессии из лаунчера
  (`DELETE /session/{id}`, с подтверждением; закрывает окно чата и сбрасывает
  выбор) и автоназвание: если сессия была пустой (без заголовка), после первого
  сообщения подставляется осмысленное имя (первые 60 символов запроса,
  `PATCH /session/{id}`).

Далее:

1. **Автообновление (доделать)** — Windows готов (ключ перегенерирован без
   пароля, `createUpdaterArtifacts: true`, CI публикует `latest.json`). Осталось:
   добавить секрет `TAURI_SIGNING_PRIVATE_KEY` в репозиторий, подпись/нотаризация
   `.app` (macOS) и проверка обновления end-to-end.
