//! Модуль GUI-автоматизации.
//!
//! На данном этапе — заглушка. Планируемая реализация:
//! - получение списка открытых окон (заголовок, иконка приложения);
//! - выбор целевого окна (кликом или из списка);
//! - вставка текста в позицию курсора:
//!   - симуляция нажатий клавиш;
//!   - через буфер обмена + Ctrl/Cmd+V;
//!   - через Accessibility API (экспериментально).

use crate::state::WindowInfo;

/// Список открытых окон.
///
/// TODO: получить окна через системные API
/// (на macOS — CGWindowListCopyWindowInfo, на Windows — EnumWindows).
pub fn list_windows() -> Vec<WindowInfo> {
    Vec::new()
}
